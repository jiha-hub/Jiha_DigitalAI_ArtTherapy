# 🏗️ Architecture Report: "Loose Code" vs. "Class-Based Program"

사용자님이 말씀하신 '날개 코드(Jupyter Notebook)'와 '클래스 단위로 정리된 프로그램'의 차이점을 기술적인 관점에서 보고합니다.

## 1. 날개 코드 (Jupyter Notebook / Script)
원본 논문의 코드와 같은 방식입니다.
- **휘발성**: 코드가 위에서 아래로 한 번 실행되고 나면, 메모리에 모델이 계속 남아있어 관리가 어렵습니다.
- **비효율성**: 기능을 하나 고치려면 관련된 모든 셀을 다시 실행해야 하며, 코드가 중복되기 쉽습니다.
- **사용자 비친화**: 모델 파라미터(시드, 강도 등)를 바꾸려면 코드 텍스트를 직접 수정해야 하므로 전문가가 아니면 사용하기 어렵습니다.

## 2. 클래스 단위 정리 (Class-Based / Modular Program)
현재 제가 구현해 드린 전산화 프로젝트 방식입니다.
- **모듈화 (Modularization)**: `model_engine.py`에서 AI 로직을 전담하고, `app.py`에서 인터페이스를 전담합니다. 레고 블록처럼 각 파트가 독립적입니다.
- **상태 관리 (State Management)**: `ArtTherapyModel` 클래스가 GPU 메모리를 관리합니다. 생성 모델을 쓰다가 인페인팅 모델로 바꿀 때, 기존 모델을 자동으로 청소하여 저사양 PC에서도 돌아가도록 최적화했습니다.
- **재사용성 및 확장성**: 나중에 새로운 AI 모델을 추가하고 싶을 때, 클래스 안에 함수 하나만 더 만들면 되어 확장이 매우 쉽습니다.
- **추상화 (Abstraction)**: 사용자는 어려운 파이썬 코드를 볼 필요 없이, 클래스가 제공하는 인터페이스(버튼, 슬라이더)만 조절하면 됩니다.

---
**결론**: '날개 코드'가 실험용 프로토타입이라면, '클래스 정리 프로그램'은 누구나 안정적으로 사용할 수 있는 **상용 수준의 소프트웨어**입니다.
