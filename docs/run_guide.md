# 🚀 마인드팔레트(MindPalette) 실행 가이드

모든 설정이 완료되었습니다. 아래 코드를 터미널에 순서대로 혹은 한 번에 입력하여 서비스를 실행하세요.

### 1단계: 프로젝트 폴더로 이동 및 실행 (중요)
터미널에서 먼저 소스 코드가 있는 폴더로 이동해야 합니다. 
```powershell
cd C:\Users\asia\Desktop\Workshop1
conda activate art_therapy
python src/app.py
```

### 2단계: 의존성 설치 (최초 1회 또는 오류 시)
`requirements.txt`가 없다는 메세지가 나오면, 반드시 위 `cd` 명령어로 폴더 이동을 했는지 확인하세요.
```powershell
cd C:\Users\asia\Desktop\Workshop1
conda activate art_therapy
pip install -r requirements.txt
```

---
**💡 팁:** 
실행 후 터미널에 뜨는 `http://127.0.0.1:7860` 주소를 브라우저에 입력하면 접속됩니다.
휴대폰에서도 접속하고 싶다면 실행 시 코드를 `demo.launch(share=True)`로 변경하여 일시적인 외부 링크를 생성할 수 있습니다.
